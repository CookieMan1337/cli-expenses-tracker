# LedgerLite — Personal Finance Tracker (CLI)

Консольное приложение для учёта личных финансов. Позволяет отслеживать доходы и расходы, управлять категориями, устанавливать бюджеты, строить отчёты и экспортировать данные.
(в соавторстве с биг дик китайская нейросеть продакшн, это был ад).
---

## 📋 Основные возможности

### Управление транзакциями
- **Добавление дохода** — `ADD_INCOME`
- **Добавление расхода** — `ADD_EXPENSE`
- **Просмотр всех транзакций** — `LIST`
- **Удаление транзакции (возможность удаления последней транзакции)** — `REMOVE`


### Управление категориями
- **Добавление категории** — `ADD_CATEGORY`
- **Просмотр всех категорий** — `LIST_CATEGORY`

### Бюджеты
- **Установка бюджета на категорию и месяц** — `BUDGET_SET`
- **Просмотр всех бюджетов** — `BUDGET_LIST`
- **Автоматический контроль превышения** при добавлении расхода

### Отчёты
- **Отчёт за текущий месяц** — `REPORT_MONTH`
- **Топ-10 расходов** — `REPORT_TOP`

### Экспорт
- **Экспорт месячного отчёта в CSV** — `EXPORT_CSV`
- **Экспорт месячного отчёта в JSON** — `EXPORT_JSON`
- **Экспорт топ-10 расходов в CSV** — `EXPORT_TOP_CSV`

### Импорт
- **Импорт транзакций из CSV** — `IMPORT_CSV`  
  Формат: `дата,тип,категория,сумма,заметка`  
  Пример: `2026-02-10,expense,FOOD,1250.50,Продукты`

### Данные
- **Автосохранение** — данные сохраняются при выходе
- **Автозагрузка** — данные восстанавливаются при запуске

---
## 🚀 Доп инфо

Сделал только gradle билд, в maven времени не хватило. В тестах особо не разбирался, тут чисто творчество дипсика, хорошо что даботает.
Возможно где то логирование не учел, уже совсем времени мало доделывать, после код ревью переделаю всё.
